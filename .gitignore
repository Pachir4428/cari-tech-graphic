# Leads reais dos clientes — nunca versionar
dados/leads.csv
dados/leads.json
dados/*.csv
dados/*.json

# Dependências PHP (PHPMailer via composer)
/vendor/
composer.phar

# Sistema
.DS_Store
Thumbs.db
*.log

# Imagens carregadas no servidor (logótipos e imagens de conteúdo)
uploads/logo-*
uploads/img-*

# Ficheiros carregados em tempo de execução (entregas e briefs de clientes)
uploads/entregas/
uploads/cliente/
